name: Tests

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "pypy-3.10"]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip wheel
          python -m pip install -e .[signals] -r requirements-dev.txt

      - name: Run dynamodb_local
        run: |
          wget --quiet http://dynamodb-local.s3-website-us-west-2.amazonaws.com/dynamodb_local_latest.tar.gz -O /tmp/dynamodb_local_latest.tar.gz
          tar -xzf /tmp/dynamodb_local_latest.tar.gz -C /tmp
          java -Djava.library.path=/tmp/DynamoDBLocal_lib -jar /tmp/DynamoDBLocal.jar -inMemory -port 8000 &

      - name: Run tests
        run: |
          pytest --cov-report term-missing --cov=aiopynamodb tests

  # TODO: enable mypy after completing type hints migration
  # mypy:
  #   runs-on: ubuntu-22.04
  #
  #   steps:
  #   - uses: actions/checkout@v4
  #   - name: Set up Python 3.12
  #     uses: actions/setup-python@v5
  #     with:
  #       python-version: '3.12'
  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       python -m pip install -e .[signals] -r requirements-dev.txt -r requirements-typecheck.txt
  #   - name: Run mypy
  #     run: |
  #       mypy .
